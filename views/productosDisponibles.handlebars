<div class="container-fluid">
  {{!-- {{> navbarInicio }} ESTA ES EL NAVBAR QUE LE CORRESPONDE, PERO MIENTRAS DESARROLLAMOS ESTARA COMENTADO POR COMODIDAD PARA LAS PRUEBAS}} --}}
  {{> buscador }}
  <div class="biombo pb-5 px-5">
    <div class="biombo row">
      
      <h1 class="text-center mt-5" style="color: green;">Producto encontrado</h1>    
        <div class="container responsivo col-6 mt-5 py-5 text-center rounded-start" style="background: aliceblue;">
            {{> cardEncontrado }}
        </div>
        
        <div class="container responsivo col-6 mt-5 py-5 rounded-end" style="background: aliceblue;">
            <section class="mt-5">
                <h1>{{this.marca}}</h1>
                <h1>{{this.nombre}}</h1>
                <h1>{{this.sexo}}</h1>             
            </section>
            <h2>para ver detalles inicia sesion</h2><br>            
            <a class="btn btn-primary mb-4 py-2 w-40 m-auto"  href="./inicio">Inicia sesion</a>
            <h2>Aun no estas registrado?</h2>
        </div> 
      
    </div>
  </div>

  <section>
    <h1 class="text-center mt-5" style="color: green;">Productos disponibles</h1>
    <div class="py-4">  
  <hr class="w-100"/>

  <table class="table w-80 m-auto rounded" style="background: green; height: 60px">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Imagen</th>
        <th scope="col">Marca</th>        
        <th scope="col">Nombre</th>
        <th scope="col">Precio</th>
        <th scope="col">Descripción</th>                     
      </tr>
    </thead>

    <tbody> 
      
      <tr>
        <td>{{@index}}</td>
        <td>
          <div>                
            <img class="rounded-circle" src="/uploads/{{this.nombre}}.jpg" height="55" Width="75">                
          </div>
        </td>
        <td>{{this.marca}}</td>
        <td>{{this.nombre}}<br></td>
        <td>{{this.precio}}</td>
        <td>{{this.sexo}}</td>

        
        
      </tr>  
      
    </tbody>
  </table>
  <div class="text-center mt-3">    
    <a class="btn btn-primary mb-4 py-2 w-40 m-auto"  href="./">Ir al buscador</a>
  </div>
    
</div>


<script>

  const autorizar = async (id, e) => {    
    const seleccion = e.checked
    console.log(id, seleccion);
    try {
      await axios.put('/seleccion', {
        id,
        seleccion
      })
      alert(seleccion ? 'Producto seleccionado, por favor confirme seleccion' : 'Ha ocurrido un error, intente más tarde');      
    }
    catch ({ respuesta }) {
      const { data } = respuesta
      const { error } = data
      alert(error)
    }    
  }
  
</script>  

  </section>
</div>

<script>

  function previewFile() {
    const preview = document.getElementById('pw');
    const file = document.querySelector('input[type=file').files[0];
    const reader = new FileReader();

    reader.addEventListener("load", () => {
      preview.src = reader.result;
    }, false);

    if (file) {
      reader.readAsDataURL(file);
    }
  }
  
</script>




















